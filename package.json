{
  "name": "lw5-wheel",
  "version": "1.0.0",
  "description": "- 单文件可部署：`index.html` - 纯前端（无后端），手机/微信内置浏览器可用 - 抽奖逻辑：**先按权重选结果**，再计算角度并动画精准停在该扇区，保证“动画=结果”一致 - 音效：按要求预留 base64 内嵌变量；同时提供 WebAudio 兜底（你不替换也能有声音）",
  "main": "sw.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alalei0921/lw5-wheel.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/alalei0921/lw5-wheel/issues"
  },
  "homepage": "https://github.com/alalei0921/lw5-wheel#readme",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2"
  }
}
